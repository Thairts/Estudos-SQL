-- Explorando dados de clientes e vendas com comandos básicos em SQL:
-- SELECT, FROM, WHERE, ORDER BY, LIMIT, DISTINCT e filtros com NOT NULL.

-- Consultas:
-- Selecionar o nome das cidades únicas no estado de Minas Gerais, organizadas em ordem alfabética.
-- Obter os 10 registros de compras mais recentes (pagas), ordenadas pela data da compra.
-- Listar os 10 clientes com maior score que nasceram a partir de 01/01/2000.

SELECT DISTINCT city
FROM sales.customers
WHERE state = 'MG'
ORDER BY city;

SELECT visit_id
FROM sales.funnel
WHERE paid_date IS NOT NULL
ORDER BY paid_date DESC
LIMIT 10;

SELECT *
FROM sales.customers
WHERE birth_date >= '2000-01-01'
ORDER BY score DESC
LIMIT 10;



